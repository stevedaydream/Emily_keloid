SUPABASE_URL=
SUPABASE_ANON_KEY=
APP_SHARED_PASSWORD=
GEMINI_API_KEY=

# LINE 轉接層（GAS）呼叫平台 API 用的共用密鑰。GAS 指令碼屬性要填同一組。
# 這組是開發測試用，正式上線請自行更換並同步到 Vercel 環境變數。
LINE_RELAY_SECRET=

# LINE 官方帳號的 basic id（形如 @abc1234），用來產生「掃碼即綁」的連結；未設定時只顯示綁定碼。
LINE_OA_BASIC_ID=