# 上線前總體檢（2026-08-14）

> 目標：真正收真病人、全功能上線（含 LINE 三項）。本檔＝**最後決議** ＋ **待助理/部長回覆** ＋ 待辦清單。
> 方法：主線用瀏覽器實走本機 dev（最新程式碼），後台與匯入用讀碼＋查資料庫。體檢過程只記錄、不修改。
> 適用角色：**助理**（建檔·基本資料·同意書·LINE綁定·匯出）／**護理師**（病灶測量·標部位·拍照·追蹤登打·放療標記）／
> **部長**（VSS·JSS·治療紀錄·診斷碼）／**病人**（平板自填）。

---

## 一、今天定案的決議（不再討論）

| # | 項目 | 決議 |
|---|---|---|
| 1 | 上線定義 | 真正收真病人、全功能，**LINE 三項（綁定／提醒／衛教 AI 問答）第一天全開** |
| 2 | 追蹤期 | 正規收 **2 年**。匯出的 **FW25–FW27 是延後回診的備用格**，不是要追 27 個月 |
| 3 | 追蹤範本 | 維持 5 點到第 12 個月，**第二年不主動排**，靠自然回診 |
| 4 | 舊病人（92 位） | **不進系統**。只收上線後的新病人，舊資料繼續用原本的 Excel 分析 |
| 5 | 平板隔離 | 改為**一次性連結**：token 綁單一個案＋逾時，平板不再持有共用帳號 session |
| 6 | 飲食運動問卷 | **先停用**，正式題目到位再從後台建 |
| 7 | 上線切換 | 當天清空 19 張個案表＋Storage 照片，設定類全留；清前先匯出備份；本機病歷號對照 CSV 換新 |
| 8 | 資料庫權限 | anon 全開**維持現狀**，只記錄為已知取捨 |
| 9 | Year 2 欄名 | `F20_time`／`Recurrence20` 是部長原檔筆誤，**維持輸出修正後的名稱**，不改回 |
| 10 | 舊表 Family 欄 | **捨棄舊版**，不轉換也不回填 |

### 外部前置（不在程式範圍內，你/醫院要辦）
- IRB：已通過或送審中 ✓
- LINE 官方帳號：已申請 ✓ → 還差 Messaging API channel、access token、webhook URL
- 醫院網路：部長筆電連 wifi 無限制 ✓（原本擔心的資訊室關卡實質解除）

---

## 二、阻損項（不做完不能收真病人）

| # | 項目 | 角色 | 說明 |
|---|---|---|---|
| B1 | **今天的程式改動尚未 commit／部署** | 開發 | 206 欄容量、只填年月、就診原因新碼表、尺寸匯入修正全都只在本機。而 `visit_reason` 的 migration **已經套進遠端 DB**，所以線上站現在是「舊程式＋新資料庫」的混合狀態 |
| B2 | **平板一次性連結** | 開發 | 目前 `/patient/[id]/intake` 仍受共用帳號保護＝交出去的平板是登入狀態；病人按上一頁或開新分頁就看得到全部個案、後台與匯出。**自填頁右上角還有一個「診間人員」按鈕**，直接就是逃生門 |
| B3 | **LINE 上線三件套** | 你 | ① GAS 部署取得 webhook URL ② LINE Developers 開 Messaging API channel、發 access token、停用自動回應 ③ Vercel 環境變數 `LINE_RELAY_SECRET`／`LINE_OA_BASIC_ID`／`GEMINI_API_KEY`。本機 `.env.local` 六個都有，**Vercel 上要逐一確認**。步驟見 `gas/README.md` |
| B4 | **收案頁的時程範本預設是「不套用」** | 開發 | `/intake` 的「套用追蹤時程範本」預設值是「不套用（之後再手動設定）」。助理沒特別注意的話，**每一位新病人都不會有追蹤時程，也就不會有 LINE 回診提醒**——這跟「LINE 提醒第一天就開」直接衝突。建議預設帶「標準術後追蹤」 |
| B5 | 停用飲食運動問卷 | 開發 | 決議 6 的執行動作。不停用的話它會出現在問卷選單第一個，人員可能誤填一份將來要作廢的示範題目 |
| B6 | 上線當天清空＋對照表換新 | 助理／開發 | 決議 7 的執行動作。**日期未定，待助理確認** |

---

## 三、應修（上線前最好修，不修會痛但不會擋）

### 資料正確性

| # | 項目 | 角色 | 說明 |
|---|---|---|---|
| F1 | **`KOST`（藥膏/貼片）主表欄位沒有輸入介面** | 開發 | 匯出的 `KOST_1..20` 讀的是個案層級的 `ointment_patch` 選項紀錄，但**個案頁的選項區塊只開了 4 類**（就診原因／發生原因／得知看診資訊／目前症狀），沒有藥膏貼片。現在測試資料有值是因為當初用 SQL 塞的，**上線後沒有人能填**。建議改成從該個案的「藥膏／貼片」治療紀錄彙總（那裡有 12 項含碼的下拉，已可用） |
| F2 | 「欄位缺口清單」附表內容過時 | 開發 | 三處都不對了：仍寫 `KC_1..5`／`KOST_1..5`（已擴到 20）、仍說「系統尚無藥膏/貼片的 1-12 清單」（**其實有，12 項都有碼**）、仍指向 `pending.md D5`（08-13 已結案）。部長看到會誤以為系統少了東西 |
| F3 | 問卷「填寫人」一律記成「診間人員」 | 開發 | 匯出的「問卷逐題作答」附表填寫人欄不記實際操作者，**部長填的 VSS 與護理師填的分不出來**。已選了操作者卻沒用上，稽核意義打折 |
| F4 | `Radiation treatment` 時程範本是空殼 | 開發／助理 | 這份範本一個項目都沒有，套用了等於沒套。要嘛補項目、要嘛刪掉，別留著讓人誤選 |
| F5 | `dev_mobile_mapping` 還開著 | 開發 | 「系統管理者」這個操作者的旗標是 true，後台 UI 自己寫著「正式收案前請關閉」 |
| F6 | 「標準術後追蹤（示範）」名稱 | 開發 | 拿掉「（示範）」，否則助理不確定這是不是正式的 |

### 使用流暢度（分角色）

| # | 角色 | 問題 |
|---|---|---|
| U1 | **護理師** | 病灶測量＋人形圖＋拍照是護理師的主戰場，但整塊藏在「病人基本資料」區塊**裡面**（標題是「蟹足腫部位與大小」），而**導覽列的 12 個區塊沒有它的入口**。每次都要先進基本資料再往下捲 |
| U2 | **部長** | 登入落點被設成 `/intake`（連續收案頁），但分工上部長不建檔，他要的是 VSS/JSS 與治療紀錄。落點應改成個案列表或今日門診 |
| U3 | **部長** | 個案頁 14 個區塊裡，「治療紀錄」排第 8、「問卷填寫」排第 13，部長要用的兩塊隔很遠，中間夾著助理與護理師的區塊 |
| U4 | **全部** | 問卷送出後跳回個案頁（`?submitted=questionnaire`），**畫面上沒有任何「已送出」提示，也不會捲到問卷區塊**，人員會不確定到底存了沒有，容易重複送出 |
| U5 | **全部** | 問卷頁的「送出」按鈕是深藍黑色，全站其他主要按鈕是品牌松綠（「更新基本資料」「開始」都是綠的）。同一個系統兩套按鈕語彙 |
| U6 | **助理** | 建檔頁「年齡」與「出生日期」並存，兩個都要填。年齡可由生日推算，重複輸入且可能互相矛盾 |
| U7 | **助理／部長** | JSW score 是自由文字欄（現值「18分」），同時 JSS 問卷送出後也會自動回填。兩條路徑寫同一欄，格式容易不一致 |
| U8 | **全部** | 基本資料區塊的家族史標籤是英文「Family（家族史）」，其餘欄位全中文 |
| U9 | **病人** | `/patient/[id]/questionnaire` **有完整導覽列**（今日門診／後台管理／資料匯出都在），`/patient/[id]/intake` 才是無導覽的全螢幕。同樣掛在 `patient` 路徑下卻兩種待遇，容易誤把問卷頁交給病人 |
| U10 | 助理 | 今日門診的搜尋提示寫「可打研究編號、**姓名**、醫師或部位」，但姓名搜尋要靠本機對照表；沒掛對照表的裝置打姓名查不到，提示會誤導 |

---

## 四、可往後放（記著就好）

- **部位碼多對一**：67 個熱區壓到部長的 22 碼，「後頸／左頸側／右頸側／下顎頸部」全部匯出成 `5`，「左上臂／左上臂後」都是 `9`。匯出後**無法還原精確部位**。這是當初刻意的設計，但部長分析時要知道。
- **未手術病人在 Year 1／Year 2 表沒有任何值**：FW 欄位以手術日為基準，只打針沒開刀的病人（實測 PU-2026-001）天數與月份都是空的，追蹤只出現在「追蹤逐筆」附表。
- **醫學術語紀錄**仍是停用狀態（程式與資料表都保留，後台維護頁還在）。
- **舊資料匯入工具**降為備用（決議 4）。今天修好的尺寸解析（新舊格式都吃）留著，不是上線路徑。
- **`diet_education`／`exercise_restriction`** 兩份示範清單還留在資料庫（各 5 項、沒有 export_code），UI 已於 07-27 移除，屬殘留。

---

## 五、待助理／部長回覆

| # | 問題 | 為什麼要問 |
|---|---|---|
| Q1 | **飲食運動習慣問卷的正式題目與選項** | 現在是示範版 5 題，已決議先停用。拿到題目後從後台自己建即可，**不用改程式** |
| Q2 | **上線日期，以及切換當天誰執行** | 清空資料庫＋本機對照表換新要有人按。清空後 5 筆測試資料與體檢期間產生的資料都會消失 |
| Q3 | **部位碼多對一可以接受嗎** | 左頸/右頸/後頸/下顎會全部變成同一碼 `5`，上臂前後不分。若研究需要分左右或前後，要另外討論怎麼編 |
| Q4 | **只打針、沒開刀的病人，追蹤要不要進 Year 1／Year 2 表** | 目前不會（那兩張表以手術日為基準）。若部長想看非手術病人的兩年追蹤，格式要調整 |
| Q5 | **第二年不主動排追蹤，部長接受嗎** | Year 2 那張 50 欄的表會有大量 0，且第二年失聯率會比有提醒時高 |
| Q6 | **`Radiation treatment` 時程範本要補內容還是刪掉** | 目前是空殼，套用了等於沒套 |

---

## 六、已知取捨（寫下來，日後不要再當成 bug）

1. **資料庫 anon 角色對所有表全開讀寫**。緩解：anon key 從未進到瀏覽器（環境變數沒有 `NEXT_PUBLIC_` 前綴，全站零個 client component 直接呼叫 Supabase），防護靠環境變數不外洩。IRB 若追問，這是目前的答案。
2. **第二年不主動排追蹤**，Year 2 資料靠病人自然回診。
3. **舊病人不進系統**，兩邊資料永久分家。
4. **FW25–27 只是備用格**，不代表追蹤 27 個月。
5. **病歷號對照只能在桌機版 Chrome/Edge**（File System Access API），手機平板一律無法寫入。

---

## 七、這次實際驗過、可以放心的部分

| 驗證項目 | 結果 |
|---|---|
| 27 條主要路由 | 全部 200（`/dashboard` 是 307 轉址，正常） |
| 匯出檔結構 | **13 張工作表**；Basic Info. **206 欄**、Operation **28**、Year 1 **48**、Year 2 **50**，末欄是 `Recurrence_27` ✓ |
| 今天改的容量 | 病灶開到 `Keloid Lo_20`、放療 `Keloid Lo_R4`、`FW27_time` 都實際產出 ✓ |
| 今天改的年月輸入 | 個案頁顯示為「2024年06月」月份選擇器 ✓ |
| VSS 端到端 | 填 4 題（2/2/3/2）→ 主表 `VSS score = 9` → 問卷分數附表逐項 → 逐題作答附表 4 列 ✓ |
| 病灶測量附表 | 9 列，長寬高／最大徑／面積／體積計算正確 ✓ |
| 追蹤逐筆附表 | 距手術天數與「第幾個月」對應正確（含跳月案例 YEN-2026-002 落在第 1、2、5 個月）✓ |
| 部位編碼 | 67 個啟用熱區 **export_code 全部有值**，無漏編 ✓ |
| LINE 端點 | 未帶密鑰回 401、錯誤方法回 405，擋得正確 ✓ |
| 就診原因新碼表 | 資料庫已是新版 6 項，「其他」＝6 ✓ |

---

## 八、上線 runbook（決議 7 的執行順序）

1. 開分支、commit 今天的 13 個檔案 ＋ 新增的 `onsetMonth.ts`、`20260814010000` migration，部署
2. Vercel 環境變數逐一確認（六個）
3. 完成 LINE 三件套（B3），實際用一支手機走一次：加好友 → 綁定碼 → 收到提醒 → 問一題衛教
4. 修完阻損項 B2、B4、B5
5. **匯出一份現有測試資料的 xlsx 留底**
6. 清空 19 張個案表＋Storage 照片（設定類不動）
7. 後台逐頁確認設定還在：醫師 2 位、放射科醫師 7 位、部位 67 個、ICD、問卷 4 份（飲食運動已停用）、時程範本、衛教 15 則
8. 本機病歷號對照 CSV 換成新檔
9. 助理建第一位真病人，走完整流程一次再正式開收

---

> 體檢期間在 `YEN-2026-001` 上新增了一筆 VSS 回覆（2026-08-14，總分 9），是端到端驗證留下的，
> 會在步驟 6 隨測試資料一起清掉。
